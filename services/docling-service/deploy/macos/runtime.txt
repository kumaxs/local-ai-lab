# Reuse the portable local-engine set. It intentionally omits Ray, KFP, and
# remote queue services that are not used by this single-host release.
-r ../docker/backend-runtime.txt

# Apple-native quality engines. Environment markers keep Intel installation
# valid while the formula policy falls back to Transformers there.
mlx-vlm==0.5.0; platform_machine == "arm64"
ocrmac==1.0.1
